# Readers and Research Zone

This zone converts allowed documents into normalized text, chunks, citations,
repository records, hashes, and searchable evidence. MarkItDown is the first
normalizer candidate, Haystack and RAGFlow are pipeline references, and Qdrant
is the optional vector sidecar.

Every answer retains source identity. Retrieved text is data, never trusted
system instructions.
